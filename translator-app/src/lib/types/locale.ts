export type Dictionary = {
    companyName: string;
    components: {
        header: {
            title: string;
            github: string;
        };
        footer: {
            libre: string;
            flatIcon: string;
        };
    };
};
